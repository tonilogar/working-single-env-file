const fs = require("fs");
const path = require("path");
const app = require("../server");
const port = process.env.PORT_BACKEND_AUTH0_DOCKER || 3000;

app.listen(port, () => console.log(`Server listening on port ${port}`));
